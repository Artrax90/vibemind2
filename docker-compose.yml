version: '3.8'

services:
  vibemind:
    build: .
    container_name: vibemind_app
    ports:
      - "3344:3344"
    volumes:
      - vibemind_storage:/storage
    restart: always

volumes:
  vibemind_storage:
